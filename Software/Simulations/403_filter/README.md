Four-Pole Analog Filter Frequency Response Notebook

This folder contains `403_filter_frequency_response.ipynb`, which models two cascaded second-order lowpass sections wrapped in a single feedback loop. Use the notebook to explore how feedback gain `k` affects peaking and stability.

Dependencies: see `requirements.txt`.
